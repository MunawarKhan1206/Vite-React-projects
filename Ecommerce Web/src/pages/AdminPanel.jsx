
const AdminPanel = () => {
  const handleAddProduct = () => {
    // Logic to add product
  };

  return (
    <div>
      <h1>Admin Panel</h1>
      <button onClick={handleAddProduct}>Add Product</button>
      {/* Add functionality to view orders */}
    </div>
  );
};

export default AdminPanel;
